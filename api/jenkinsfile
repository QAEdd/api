pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                dir('api') {
                    sh "rm application/tests/test_int*"
                    sh "bash test_basic.sh"
                }
            }

        }
    }
}